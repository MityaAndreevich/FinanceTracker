# Fixture: store as written by 1.0.5 (build 9)

**Evidence about a released binary, not a store we constructed.** Generated by
`scripts/capture-store-fixture.sh v1.0.5-build9 V1_0_5`.

| | |
|---|---|
| commit | `b908d2d` — chore(release): bump to 1.0.5 (build 9) |
| commit date | 2026-08-16 00:36:03 +0300 |
| marketing version | `1.0.5` (build 9) |
| captured | 2026-08-16 on iPhone 17 Pro Max, erased simulator, `--demo-mode`, Debug build |
| transactions | 33 (`sum(ZAMOUNTCENTS)` = 635473) |
| categories / sources | 13 / 3 |
| tables | `ACHANGE ATRANSACTION ATRANSACTIONSTRING ZCATEGORY ZMERCHANTCATEGORYLEARNING ZSOURCE ZTRANSACTION ZTRANSACTIONSPLIT Z_METADATA Z_MODELCACHE Z_PRIMARYKEY ` |

`ZTRANSACTION` columns:

```
Z_PK Z_ENT Z_OPT ZAMOUNTCENTS ZISDEMO ZISPOSSIBLEDUPLICATE ZTAXCENTS ZCATEGORY ZSOURCE ZCREATEDAT ZDATE ZUPDATEDAT ZCURRENCY ZMERCHANT ZNOTE ZRECURRENCERAW ZTYPERAW ZUUID 
```

## Rules

- **Never open this store in place** — an open MIGRATES it, destroying the
  property the fixture exists for. Tests stage a copy in a temp directory.
- Reading it with `sqlite3` rewrites the `-shm`/`-wal` sidecars; the
  `.sqlite` is what carries the evidence.
- To re-derive: `FORCE=1 scripts/capture-store-fixture.sh v1.0.5-build9 V1_0_5`.
